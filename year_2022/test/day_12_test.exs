defmodule Day12Test do
  use ExUnit.Case, async: true

  test "it can solve part one" do
    assert Day12.part_one(InputTestFile) == nil
  end

  @tag :skip
  test "it can solve part two" do
    assert Day12.part_two(InputTestFile) == nil
  end
end
